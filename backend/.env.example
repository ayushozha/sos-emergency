# SOS Emergency backend — server-side secrets
# Copy to backend/.env and fill in. This file (.env) is gitignored.

# Required. Get a key at https://featherless.ai/
FEATHERLESS_API_KEY=

# Optional overrides
# Must be a non-reasoning, ungated instruct model that emits A2UI JSON in
# delta.content. Reasoning models (e.g. GLM-*) put output in delta.reasoning and
# render nothing; gated models (e.g. meta-llama/*) return model_gated_needs_oauth.
# FEATHERLESS_MODEL=Qwen/Qwen2.5-72B-Instruct
# FEATHERLESS_BASE_URL=https://api.featherless.ai/v1

# Comma-separated allowed origins. "*" is fine for local dev; lock down for prod.
# CORS_ALLOW_ORIGINS=http://localhost:3000,https://your-app.example.com

# --- Deepgram Voice Agent (the hands-free voice layer) ---

# Required for /v1/voice/agent. Get a key at https://deepgram.com/
DEEPGRAM_API_KEY=

# Optional voice overrides (defaults shown)
# VOICE_LISTEN_MODEL=nova-3
# VOICE_THINK_MODEL=gpt-4o-mini
# VOICE_THINK_TEMPERATURE=0.3
# VOICE_SPEAK_MODEL=aura-2-thalia-en
# VOICE_LANGUAGE=en
# VOICE_GREETING=Emergency assistant here. Tell me what's happening.
# VOICE_INPUT_SAMPLE_RATE=16000
# VOICE_OUTPUT_SAMPLE_RATE=24000
# VOICE_DISPATCHER_PROMPT=   # override the built-in emergency-dispatcher prompt